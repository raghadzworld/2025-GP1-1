<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

<!-- منبّهات التذكيرات: تشتغل حتى لو التطبيق مقفول بالكامل، عن طريق
     AlarmManager (حزمة android_alarm_manager_plus). RECEIVE_BOOT_COMPLETED
     يخلي rescheduleOnReboot يشتغل صح بعد إعادة تشغيل الجوال، و
     SCHEDULE_EXACT_ALARM لازم لدقة توقيت المنبّه على أندرويد ١٢+. -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.VIBRATE" />
<!-- إشعار التذكير يستخدم fullScreenIntent: true (يوقظ الشاشة زي المنبّه).
     من أندرويد ١٤ هذي الصلاحية ما تُمنح تلقائيًا إلا لتطبيقات المنبّه
     والاتصال؛ بدون التصريح فيها هنا يتجاهل النظام fullScreenIntent بصمت
     ويعرض الإشعار كإشعار عادي. -->
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

<!-- تزويد الساعة بشبكة الواي فاي عبر BLE -->
<uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
<uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
<uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <application
android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
              
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="nabeeh"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_background_service's own manifest doesn't declare a
             foregroundServiceType, so Android 14+ throws a SecurityException
             (silently swallowed by the plugin) when we start it. Declaring it
             here fixes that — dataSync only: connectedDevice additionally
             requires a Bluetooth/USB/NFC permission held at runtime, which
             doesn't apply to our plain Wi-Fi/TCP connection to the watch and
             was itself the cause of a separate SecurityException crash. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- ‏android_alarm_manager_plus ما يصرّح عن مكوّناته في مانيفست الحزمة
             نفسها (على عكس أغلب الحِزَم) — لازم التطبيق يصرّح عنها بنفسه، وهذا
             موثّق بـ README الحزمة كخطوة إعداد إجبارية. بدونها الجدولة "تنجح"
             ظاهريًا: AlarmManager يسجّل الموعد ويطلقه بوقته، لكن البثّ يروح
             لمُستقبِل غير موجود بالتطبيق فما ينفّذ reminderAlarmCallback ولا
             يظهر أي إشعار — وهذا بالضبط سبب إن التذكيرات ما تشتغل والتطبيق
             مقفول. -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />
        <!-- ‏enabled=false مقصود: الحزمة تفعّل هذا المُستقبِل وقت التشغيل أول ما
             تُجدول أول منبّه بـ rescheduleOnReboot: true، عشان ما ياخذ
             BOOT_COMPLETED بدون داعي. -->
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- مُستقبِلات flutter_local_notifications: هذي هي اللي تعرض إشعار
             التذكير وقت موعده. كود أصلي (Java) يشتغل من النظام مباشرة، بدون
             ما يحتاج التطبيق يكون شغّالًا ولا يفتح عزلة Dart خلفية — وهذا
             اللي يخلي التذكيرات المجدولة (zonedSchedule) موثوقة. الحزمة ما
             تصرّح عنها بنفسها، لازم التطبيق يصرّح عنها زي حزمة المنبّهات.
             ScheduledNotificationBootReceiver يعيد ضبط المواعيد بعد إعادة
             تشغيل الجوال أو بعد تحديث التطبيق. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="sms"/>
        </intent>
    </queries>
</manifest>
